{
  "name": "sync",
  "version": "0.0.0",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev --local -e dev",
    "deploy": "wrangler deploy",
    "tail-logs": "wrangler tail sync"
  },
  "dependencies": {
    "@cadence/cal-store": "workspace:^",
    "date-fns": "^2.30.0",
    "http2": "npm:is_number",
    "toucan-js": "^3.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230511.0",
    "esbuild-plugin-ignore": "^1.1.1",
    "wrangler": "^3.0.0"
  },
  "private": true
}
