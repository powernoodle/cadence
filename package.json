{
  "name": "cadence",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "git config --local core.hooksPath .githooks && brew install aws/tap/aws-sam-cli",
    "nx": "nx",
    "check": "pnpm install --frozen-lockfile",
    "build": "nx run-many -t build",
    "dev": "nx run-many -t dev"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@sentry/cli": "^2.18.1",
    "jest": "^29.5.0",
    "nx": "16.0.1",
    "nx-cloud": "latest",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.1"
  },
  "optionalDependencies": {
    "@miniflare/tre": "3.0.0-next.13",
    "miniflare": "^3.0.0"
  }
}
